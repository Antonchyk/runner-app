import { ClockTimeDirective } from './clock-time.directive';

describe('ClockTimeDirective', () => {
  it('should create an instance', () => {
    const directive = new ClockTimeDirective();
    expect(directive).toBeTruthy();
  });
});
